function setupDiscreteMathPractice({ root, format }) {
    const key = "mathengine-discrete-math-practice-v1", labels = { temelji: "Temelji", primjena: "Primjena", izazov: "Izazov" };
    let bank, difficulty = "temelji", current, previous, state = load();
    root.innerHTML = `<div class="cb-practice-heading"><div><span class="discrete-math-chip">Skupovi i grafovi</span><h2>Vježbaonica diskretne matematike</h2><p>Vježbaj skupove i relacije, teoriju grafova, rekurzije i naprednu kombinatoriku.</p></div><div class="cb-practice-level"><span>Bodovi</span><strong id="dm-practice-points">0</strong></div></div><div class="cb-practice-stats"><div><span>Točno</span><strong id="dm-practice-correct">0</strong></div><div><span>Točnost</span><strong id="dm-practice-accuracy">—</strong></div><div><span>Najbolji niz</span><strong id="dm-practice-best">0</strong></div></div><div class="cb-practice-difficulties"><button data-dm-difficulty="temelji" class="active">Temelji</button><button data-dm-difficulty="primjena">Primjena</button><button data-dm-difficulty="izazov">Izazov</button></div><article class="cb-practice-card"><div class="cb-practice-meta"><span id="dm-practice-topic"></span><span id="dm-practice-streak"></span></div><h3 id="dm-practice-title">Učitavam zadatke…</h3><p id="dm-practice-question"></p><div class="cb-practice-answer"><input id="dm-practice-answer" inputmode="decimal" autocomplete="off" placeholder="Upiši broj"><strong id="dm-practice-unit"></strong></div><div class="cb-practice-actions"><button id="dm-practice-check">Provjeri</button><button id="dm-practice-hint" class="secondary">Mali trag</button><button id="dm-practice-next" class="secondary">Novi zadatak</button></div><p id="dm-practice-feedback" class="cb-practice-feedback" aria-live="polite"></p><div id="dm-practice-hint-box" class="cb-practice-note" hidden></div><div id="dm-practice-solution" class="cb-practice-note solution" hidden></div></article><button id="dm-practice-reset" class="cb-practice-reset">Poništi moj napredak</button>`;
    function initial() { return { attempts: 0, correct: 0, streak: 0, best: 0, points: 0, completed: {} }; }
    function load() { try { return { ...initial(), ...JSON.parse(localStorage.getItem(key) || "{}") }; } catch (_) { return initial(); } }
    function save() { localStorage.setItem(key, JSON.stringify(state)); dashboard(); }
    function dashboard() { document.getElementById("dm-practice-points").textContent = state.points; document.getElementById("dm-practice-correct").textContent = state.correct; document.getElementById("dm-practice-accuracy").textContent = state.attempts ? `${Math.round(state.correct / state.attempts * 100)}%` : "—"; document.getElementById("dm-practice-best").textContent = state.best; }
    function choose() { if (!bank) return; let candidates = bank[difficulty].filter((item) => item.id !== previous); if (!candidates.length) candidates = bank[difficulty]; const least = Math.min(...candidates.map((item) => state.completed[item.id] || 0)); const pool = candidates.filter((item) => (state.completed[item.id] || 0) === least); current = pool[Math.floor(Math.random() * pool.length)]; previous = current.id; show(); }
    function show() { document.getElementById("dm-practice-topic").textContent = current.topic; document.getElementById("dm-practice-title").textContent = current.title; document.getElementById("dm-practice-question").textContent = current.question; document.getElementById("dm-practice-unit").textContent = current.unit; document.getElementById("dm-practice-streak").textContent = `${labels[difficulty]} · niz ${state.streak}`; document.getElementById("dm-practice-answer").value = ""; document.getElementById("dm-practice-feedback").textContent = ""; document.getElementById("dm-practice-feedback").className = "cb-practice-feedback"; document.getElementById("dm-practice-hint-box").hidden = true; document.getElementById("dm-practice-solution").hidden = true; document.getElementById("dm-practice-check").disabled = false; }
    function answer() { const value = document.getElementById("dm-practice-answer").value.trim().replace(",", "."); if (!value) throw new Error("Najprije upiši odgovor."); const parsed = Number(value); if (!Number.isFinite(parsed)) throw new Error("Odgovor mora biti broj."); return parsed; }
    function check() { if (!current) return; const feedback = document.getElementById("dm-practice-feedback"); try { const correct = Math.abs(answer() - current.answer) <= current.tolerance; state.attempts++; if (correct) { state.correct++; state.streak++; state.best = Math.max(state.best, state.streak); state.points += (difficulty === "temelji" ? 10 : difficulty === "primjena" ? 20 : 30) + Math.min(state.streak - 1, 5) * 2; state.completed[current.id] = (state.completed[current.id] || 0) + 1; feedback.textContent = `Točno! ${format(current.answer)} ${current.unit}`.trim(); feedback.classList.add("correct"); } else { state.streak = 0; feedback.textContent = "Nije još točno. Zapiši formulu, uvrsti podatke i pojednostavni izraz."; feedback.classList.add("incorrect"); } const solution = document.getElementById("dm-practice-solution"); const heading = document.createElement("strong"); heading.textContent = "Postupak"; const paragraph = document.createElement("p"); paragraph.textContent = current.explanation; solution.replaceChildren(heading, paragraph); solution.hidden = false; document.getElementById("dm-practice-check").disabled = true; save(); } catch (error) { feedback.textContent = error.message; feedback.classList.add("incorrect"); } }
    root.querySelectorAll("[data-dm-difficulty]").forEach((button) => button.addEventListener("click", () => { difficulty = button.dataset.dmDifficulty; root.querySelectorAll("[data-dm-difficulty]").forEach((item) => item.classList.toggle("active", item === button)); choose(); }));
    document.getElementById("dm-practice-check").addEventListener("click", check);
    document.getElementById("dm-practice-answer").addEventListener("keydown", (event) => { if (event.key === "Enter") check(); });
    document.getElementById("dm-practice-next").addEventListener("click", choose);
    document.getElementById("dm-practice-hint").addEventListener("click", () => { if (!current) return; const box = document.getElementById("dm-practice-hint-box"); box.textContent = current.hint; box.hidden = false; });
    document.getElementById("dm-practice-reset").addEventListener("click", () => { state = initial(); save(); choose(); });
    dashboard();
    fetch("data/discrete-math-exercises.json").then((response) => { if (!response.ok) throw new Error("Banka zadataka nije dostupna."); return response.json(); }).then((data) => { bank = data; choose(); }).catch((error) => { document.getElementById("dm-practice-title").textContent = "Zadatke nije moguće učitati"; document.getElementById("dm-practice-question").textContent = error.message; });
}
