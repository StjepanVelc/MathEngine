function setupCombinatoricsProbabilityStatisticsPractice({ root, format }) {
    const key = "mathengine-combinatorics-probability-statistics-practice-v1", labels = { temelji: "Temelji", primjena: "Primjena", izazov: "Izazov" };
    let bank, difficulty = "temelji", current, previous, state = load();
    root.innerHTML = `<div class="cps-practice-heading"><div><span class="combinatorics-probability-statistics-chip">Prebrojavanje, vjerojatnost i statistika</span><h2>Vježbaonica kombinatorike i statistike</h2><p>Vježbaj permutacije, kombinacije, vjerojatnost i deskriptivnu statistiku.</p></div><div class="cps-practice-level"><span>Bodovi</span><strong id="cps-practice-points">0</strong></div></div><div class="cps-practice-stats"><div><span>Točno</span><strong id="cps-practice-correct">0</strong></div><div><span>Točnost</span><strong id="cps-practice-accuracy">—</strong></div><div><span>Najbolji niz</span><strong id="cps-practice-best">0</strong></div></div><div class="cps-practice-difficulties"><button data-cps-difficulty="temelji" class="active">Temelji</button><button data-cps-difficulty="primjena">Primjena</button><button data-cps-difficulty="izazov">Izazov</button></div><article class="cps-practice-card"><div class="cps-practice-meta"><span id="cps-practice-topic"></span><span id="cps-practice-streak"></span></div><h3 id="cps-practice-title">Učitavam zadatke…</h3><p id="cps-practice-question"></p><div class="cps-practice-answer"><input id="cps-practice-answer" inputmode="decimal" autocomplete="off" placeholder="Upiši broj"><strong id="cps-practice-unit"></strong></div><div class="cps-practice-actions"><button id="cps-practice-check">Provjeri</button><button id="cps-practice-hint" class="secondary">Mali trag</button><button id="cps-practice-next" class="secondary">Novi zadatak</button></div><p id="cps-practice-feedback" class="cps-practice-feedback" aria-live="polite"></p><div id="cps-practice-hint-box" class="cps-practice-note" hidden></div><div id="cps-practice-solution" class="cps-practice-note solution" hidden></div></article><button id="cps-practice-reset" class="cps-practice-reset">Poništi moj napredak</button>`;
    function initial() { return { attempts: 0, correct: 0, streak: 0, best: 0, points: 0, completed: {} }; }
    function load() { try { return { ...initial(), ...JSON.parse(localStorage.getItem(key) || "{}") }; } catch (_) { return initial(); } }
    function save() { localStorage.setItem(key, JSON.stringify(state)); dashboard(); }
    function dashboard() { document.getElementById("cps-practice-points").textContent = state.points; document.getElementById("cps-practice-correct").textContent = state.correct; document.getElementById("cps-practice-accuracy").textContent = state.attempts ? `${Math.round(state.correct / state.attempts * 100)}%` : "—"; document.getElementById("cps-practice-best").textContent = state.best; }
    function choose() { if (!bank) return; let candidates = bank[difficulty].filter((item) => item.id !== previous); if (!candidates.length) candidates = bank[difficulty]; const least = Math.min(...candidates.map((item) => state.completed[item.id] || 0)); const pool = candidates.filter((item) => (state.completed[item.id] || 0) === least); current = pool[Math.floor(Math.random() * pool.length)]; previous = current.id; show(); }
    function show() { document.getElementById("cps-practice-topic").textContent = current.topic; document.getElementById("cps-practice-title").textContent = current.title; document.getElementById("cps-practice-question").textContent = current.question; document.getElementById("cps-practice-unit").textContent = current.unit; document.getElementById("cps-practice-streak").textContent = `${labels[difficulty]} · niz ${state.streak}`; document.getElementById("cps-practice-answer").value = ""; document.getElementById("cps-practice-feedback").textContent = ""; document.getElementById("cps-practice-feedback").className = "cps-practice-feedback"; document.getElementById("cps-practice-hint-box").hidden = true; document.getElementById("cps-practice-solution").hidden = true; document.getElementById("cps-practice-check").disabled = false; }
    function answer() { const text = document.getElementById("cps-practice-answer").value.trim().replace(",", "."); if (!text) throw new Error("Najprije upiši odgovor."); const value = Number(text); if (!Number.isFinite(value)) throw new Error("Odgovor mora biti broj."); return value; }
    function check() { if (!current) return; const feedback = document.getElementById("cps-practice-feedback"); try { const correct = Math.abs(answer() - current.answer) <= current.tolerance; state.attempts++; if (correct) { state.correct++; state.streak++; state.best = Math.max(state.best, state.streak); state.points += (difficulty === "temelji" ? 10 : difficulty === "primjena" ? 20 : 30) + Math.min(state.streak - 1, 5) * 2; state.completed[current.id] = (state.completed[current.id] || 0) + 1; feedback.textContent = `Točno! ${format(current.answer)} ${current.unit}`.trim(); feedback.classList.add("correct"); } else { state.streak = 0; feedback.textContent = "Nije još točno. Zapiši formulu, uvrsti podatke i pojednostavni izraz."; feedback.classList.add("incorrect"); } const solution = document.getElementById("cps-practice-solution"); const heading = document.createElement("strong"); heading.textContent = "Postupak"; const text = document.createElement("p"); text.textContent = current.explanation; solution.replaceChildren(heading, text); solution.hidden = false; document.getElementById("cps-practice-check").disabled = true; save(); } catch (error) { feedback.textContent = error.message; feedback.classList.add("incorrect"); } }
    root.querySelectorAll("[data-cps-difficulty]").forEach((button) => button.addEventListener("click", () => { difficulty = button.dataset.cpsDifficulty; root.querySelectorAll("[data-cps-difficulty]").forEach((item) => item.classList.toggle("active", item === button)); choose(); })); document.getElementById("cps-practice-check").addEventListener("click", check); document.getElementById("cps-practice-answer").addEventListener("keydown", (event) => { if (event.key === "Enter") check(); }); document.getElementById("cps-practice-next").addEventListener("click", choose); document.getElementById("cps-practice-hint").addEventListener("click", () => { if (!current) return; const box = document.getElementById("cps-practice-hint-box"); box.textContent = current.hint; box.hidden = false; }); document.getElementById("cps-practice-reset").addEventListener("click", () => { state = initial(); save(); choose(); });
    dashboard(); fetch("data/combinatorics-probability-statistics-exercises.json").then((response) => { if (!response.ok) throw new Error("Banka zadataka nije dostupna."); return response.json(); }).then((data) => { bank = data; choose(); }).catch((error) => { document.getElementById("cps-practice-title").textContent = "Zadatke nije moguće učitati"; document.getElementById("cps-practice-question").textContent = error.message; });
}
